# SpatialNexus — screenshots to capture

PNG, 1600×1000, in `docs/img/`.

| File | Shows |
|------|-------|
| `docs/img/banner.svg`         | Atlas brand banner (committed) |
| `docs/img/01-hero.png`        | Hero with compass rose + isometric graph |
| `docs/img/02-impact-form.png` | Impact query form with example chips |
| `docs/img/03-impact-result.png` | Result: nodes + edges + narrative |
| `docs/img/04-react-flow.png`  | React Flow graph explorer with the impact payload |
| `docs/img/05-subgraph-curl.png` | (Optional) `curl /v1/graph/subgraph` JSON |

Seed Neo4j with `scripts/seed.cypher` before capturing so `PUMP-A1` returns a
meaningful neighbourhood.
